import { useEffect, useState } from "react";
import { db } from "../../../../firebase/firebase-config";
import { getDocs, collection } from "firebase/firestore";

export default function MachesViewer() {

  useEffect((

  ) => { }, [])

  return (
    <section>
      <h2>Próximos Partidos</h2>
    </section>
  )
}
